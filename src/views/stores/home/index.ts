/**
 * Home Store
 */
import { defineStore } from 'pinia'

// 创建 Store
const useHomeStore = defineStore('homeStore', {
  state: () => ({}),
  getters: {},
  actions: {},
})

export default useHomeStore
